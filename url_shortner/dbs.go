package main

import (
	_ "github.com/go-sql-driver/mysql"
)

/*func main_() {
	db, err := sql.Open("mysql", "reetu:Reetu8995@@(localhost:3306)/test")
	//db.Ping()
	insert, err := db.Query("INSERT INTO db_url (path, url) VALUES ('/map', 'https://www.google.com/maps')")
	if err != nil {
		panic(err.Error())
	}

}
*/
